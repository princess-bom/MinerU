name: mineru-desktop-test

on:
  push:
    branches:
      - "master"
      - "dev"
    paths:
      - ".github/workflows/desktop.yml"
      - "apps/desktop/**"
      - "tests/unittest/pdfs/test.pdf"
  pull_request:
    paths:
      - ".github/workflows/desktop.yml"
      - "apps/desktop/**"
      - "tests/unittest/pdfs/test.pdf"

jobs:
  desktop-checks:
    timeout-minutes: 60
    strategy:
      fail-fast: true
      matrix:
        include:
          - os: macos-latest
            os_name: macos
          - os: windows-latest
            os_name: windows
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - name: Install desktop dependencies
        run: npm --prefix apps/desktop ci

      - name: Install Playwright browsers
        run: npm --prefix apps/desktop exec playwright install

      - name: Typecheck desktop app
        run: npm --prefix apps/desktop run typecheck

      - name: Run desktop tests
        run: npm --prefix apps/desktop run test

      - name: Run desktop unpackaged smoke
        run: npm --prefix apps/desktop run smoke:unpackaged

      - name: Upload desktop evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: desktop-evidence-${{ matrix.os_name }}
          path: .sisyphus/evidence/
          if-no-files-found: warn
